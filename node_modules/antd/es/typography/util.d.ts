import * as React from 'react';
export declare function measure(originEle: HTMLElement, rows: number, content: React.ReactNode, fixedContent: React.ReactNode[], ellipsisStr: string): {
    content: React.ReactNode;
    text: string;
    ellipsis: boolean;
};
